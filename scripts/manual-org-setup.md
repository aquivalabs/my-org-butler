1. External Credential → TavilyApi → add API key
2. Setup → Data Cloud → verify provisioned (wait if needed)
3. Setup → "Audit" → Einstein Generative AI → enable Agent Analytics + Session Tracing
4. Setup → Data Library → create `MyOrgButlerLibrary` (File) → upload `scripts/data/policy.pdf` → create search index
