{
  "name": "callSalesforceApi",
  "description": "Call an official public Salesforce API REST endpoint",
  "parameters": {
    "type": "object",
    "properties": {
      "urlIncludingParams": {
        "type": "string",
        "description": "The URL fragment of the REST endpoint starting with /. The Orgs base URL will be added"
      },
      "httpMethod": {
        "type": "string",
        "enum": [
          "GET",
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ],
        "description": "The HTTP method to use"
      },
      "body": {
        "type": "string",
        "description": "Payload data to be sent in the request body"
      }
    },
    "required": [
      "urlIncludingParams",
      "httpMethod"
    ]
  }
}
{
  "name": "sendNotification",
  "parameters": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "Short title of the notification"
      },
      "body": {
        "type": "string",
        "description": "Notification text"
      },
      "targetId": {
        "type": "string",
        "description": "Salesforce Id of the record to link to"
      },
      "recipientIds": {
        "type": "string",
        "description": "Comma separated list of Salesforce Ids of the users to send the notification to"
      }
    },
    "required": [
      "recipientIds",
      "title",
      "body"
    ]
  },
  "description": "Sends an In-org notification message to a set of users optionally linking to a target record"
}