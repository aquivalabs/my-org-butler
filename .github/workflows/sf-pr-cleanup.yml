name: SF PR Cleanup
run-name: "Cleanup PR #${{ github.event.pull_request.number }}"

on:
  pull_request:
    types: [closed]

jobs:
  cleanup:
    uses: aquivalabs/agentic-development/.github/workflows/sf-pr-cleanup.yml@main
    secrets: inherit
